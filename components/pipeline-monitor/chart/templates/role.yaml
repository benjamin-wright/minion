apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Values.roleName }}
rules:
- apiGroups: ["minion.ponglehub.co.uk"]
  resources: ["pipelines"]
  verbs: ["list", "get", "update", "watch"]